replicaCount: 3

image:
  repository: eniselkunch/devops-project
  tag: ""      # default, will override dynamically
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 80

containerPort: 5001
